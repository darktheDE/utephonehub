<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Login MVC Backend</display-name>

    <!-- AppContextListener for initializing services and DB connection -->
    <listener>
        <listener-class>com.example.config.AppContextListener</listener-class>
    </listener>

    <!--
        Filter registration using web.xml instead of @WebFilter annotations
        to explicitly control the order of execution.
        Filters are executed in the order they are defined in this file.
    -->

    <!-- 1. CORS Filter (runs first for all requests) -->
    <filter>
        <filter-name>CORSFilter</filter-name>
        <filter-class>com.example.filter.CORSFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>CORSFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- 2. Authentication Filter (runs after CORS for API requests) -->
    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>com.example.filter.AuthFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/api/*</url-pattern>
    </filter-mapping>

</web-app>